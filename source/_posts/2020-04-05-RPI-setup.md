---
title: 配置用于机器人项目的树莓派
date: 2020-04-05 11:53:00
tags:
---

虽然树莓派的初衷是教育领域，但它丰富的GPIO接口，廉价却完整的硬件构架使其成为了最受欢迎的机器人系统开发平台之一。废话说完了，下面开始正题。

<!--more-->

## 使用需求
在官方Raspbian桌面系统下进行开发有很多局限性：交互界面不友好、趁手的软件不支持、桌面系统占用大量CPU周期......因此，我的使用原则是在本地（Mac）进行开发，远程部署至树莓派运行，同时需要将数据传回本地。整个过程尽可能简洁。

## 安装Raspbian系统[（参考资料）](https://www.raspberrypi.org/documentation/installation/installing-images/README.md)
- 下载最新的[系统镜像](https://www.raspberrypi.org/downloads/raspbian/)。如果桌面系统不是必要，则推荐使用Lite版本。（可以减少不必要的系统中断，使对运行速度要求高的程序更稳定）
- 解压得到.img文件。
- Mac插入Micro SD卡。
- 运行`diskutil list`，找到SD卡对应的盘位，如`/dev/disk2`
- 弹出SD卡`diskutil unmountDisk /dev/diskN`。
其中`N`为SD卡的盘位。
- 烧录`sudo dd bs=1m if=path_of_your_image.img of=/dev/rdiskN conv=sync`。
其中`N`为SD卡的盘位。
其中`path_of_your_image.img`为系统镜像地址。
这一步骤需要好几分钟，可以通过`Ctrl+T`查看进度。
- 弹出SD卡`sudo diskutil eject /dev/rdiskN`。


## 配置
新安装的系统配置有4种情况：

 |	| 有显示器键鼠 | 无显示器键鼠 |
 | --- |:---:|:---:|
 | 有桌面系统 | A | B |
 | 无桌面系统 | C | D |

下面的配置以`C`情况为例。

### 安全配置
树莓派与显示器键鼠连接，第一次开机后使用默认用户名密码登录。（由于地球人都知道用户名密码，所以这个树莓派不被攻击是不科学的。）登录后首先要做的是删除默认`pi`用户。
- 添加用户
```bash
sudo adduser your_username
```
- 将新用户添加至sudo组
```bash
sudo adduser your_username sudo
```
- 检查是否添加成功，并切换至root用户
```bash
sudo su
```
- 更改root密码
```bash
sudo raspi-config // Option 1
// OR
passwd // Option 2
```
- 删除`pi`用户
```bash
exit 
// then log in with your_username
sudo deluser -remove-home pi 
// the /home/pi will be removed
```
注：如果安装的是带桌面系统的镜像，`pi`无法直接删除，因为默认登陆用户`pi`始终运行图形界面。一个简单的方法是通过`sudo raspi-config`关闭桌面系统，重启后登陆新用户，再运行上述命令删除`pi`用户。此时可以在此打开桌面系统。

- sudo取消密码输入
编辑文件`/etc/sudoers`，添加一行`your_username ALL=(ALL:ALL) NOPASSWD:ALL`：
```bash
sudo nano /etc/sudoers
```

### 网络连接[（参考资料）](https://www.raspberrypi.org/documentation/configuration/wireless/wireless-cli.md)
（如果树莓派与路由器通过网线直连，步骤1-3可以跳过。）
- 通过`sudo raspi-config`打开SSH通道，如果需要远程登陆桌面系统，则同时打开VNC通道。
- 通过`sudo raspi-config`链接wifi。
- 查看树莓派获得的IP地址
```bash
ifconfig
```
- 本地（Mac）通过ssh连接树莓派
```bash
ssh your_username@192.168.0.xxx
```
- 如果不想每次ssh都输入密码，可以将本地的ssh公钥传至树莓派（这条命令在本地而不是树莓派上执行）
```bash
ssh-copy-id -i <USERNAME>@<IP-ADDRESS>
```

- 如果树莓派重装系统后第一次ssh登陆时有冲突：
```bash
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@    WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!     @
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
IT IS POSSIBLE THAT SOMEONE IS DOING SOMETHING NASTY!
Someone could be eavesdropping on you right now (man-in-the-middle attack)!
It is also possible that a host key has just been changed.
The fingerprint for the ECDSA key sent by the remote host is
SHA256:8AYVimIa4f5PlRiqxa6E/ylMj8iKQSqq9pmrqJyUFLs.
Please contact your system administrator.
Add correct host key in /Users/anmo/.ssh/known_hosts to get rid of this message.
Offending ECDSA key in /Users/anmo/.ssh/known_hosts:7
ECDSA host key for 192.168.2.14 has changed and you have requested strict checking.
Host key verification failed.
```
则可以在本地电脑上删除旧的密钥：
```bash
ssh-keygen -R <IP_of_your_Raspberry_Pi>
```


## 远程操作
树莓派作为机器人控制系统，需要频繁调试代码，传输至树莓派上编译运行，并将数据取回本地。

### 代码

#### VS Code + Remote - SSH Extension
目前最尊贵的方法，没有之一。

#### Sublime Text 3 + SFTP

我曾今的方案是[Sublime Text 3](https://www.sublimetext.com/) + [SFTP](https://codexns.io/product/sftp_for_sublime)。这个方案可以实现一个快捷键完成代码上传，不需要切换窗口。
- 在`Sublime Text 3`环境下添加`package SFTP`
- 在代码目录新建配置文件{% asset_link sftp-config sftp-config.json %}
```
    "host": "your_RPI_IP_address",
    "user": "your_username",
    "password": "your_password",
    //"port": "22",
    "remote_path": "/home/your_username/your_destination/",
```
- 编辑代码后，通过快捷键将当前文件传输至树莓派指定的文件夹中
- `ctrl+cmd+(u, f)` current file, `ctrl+cmd+(u, n)` opened files, `ctrl+cmd+(u, r)` entire folder
- 最后切换窗口至命令行，ssh登陆树莓派编译运行程序

### 数据
通常我将树莓派生成的数据保存在树莓派，再通过[rsycn](https://www.digitalocean.com/community/tutorials/how-to-use-rsync-to-sync-local-and-remote-directories)同步至本地：
```bash
rsync -azvP your_username@192.168.0.2:/home/your_username/data/ /Users/anmo/Documents/lso/rawData
```

如果有调试需要，也可以利用`piping`将数据引导至本地文件或直接进行后处理：
```bash
ssh your_username@192.168.0.38 ./a.out | python plot.py
```


## 实时操作系统
树莓派系统不是实时操作系统，无法满足确定的（deterministic）低延时响应。目前可以通过安装精简系统、安装RT_Preempt补丁的方式尽可能提高实时性。

- 以Linux内核版本4.19为例，按照上述配置方式安装[精简版Raspbian系统](https://downloads.raspberrypi.org/raspbian_lite/images/raspbian_lite-2019-07-12/)
- （以下操作在树莓派上操作）
<!-- - 安装必要工具
```bash
sudo apt -y update # do not upgrade!
sudo apt -y install git
``` -->

- 下载编译好的RT_Preempt补丁（也可以[自己编译](https://lemariva.com/blog/2019/09/raspberry-pi-4b-preempt-rt-kernel-419y-performance-test
)）并解压
```bash
cd /tmp
wget https://github.com/lemariva/RT-Tools-RPi/raw/master/preempt-rt/kernel_4_19_59-rt23-v7l%2B/rt-kernel.tgz
tar xzf rt-kernel.tgz
cd boot
sudo cp -rd * /boot/
cd ../lib
sudo cp -dr * /lib/
cd ../overlays
sudo cp -d * /boot/overlays
cd ..
sudo cp -d bcm* /boot/
```
- 将`kernel=kernel7_rt.img`添加一行到`/boot/config.txt`:
```bashkernel=kernel7_rt.img
# Add the option: kernel=kernel7_rt.img
sudo nano /boot/config.txt
```
- 重启树莓派，查看补丁是否安装成功
```bash
~$ uname -r
4.19.59-rt23-v7l+
```


<!-- # Shortcut -->
<!-- # git clone git@github.com:moanan/OS_setup.git -->

### 参考资料
https://github.com/lemariva/RT-Tools-RPi/tree/master/preempt-rt/kernel_4_19_59-rt23-v7l%2B
https://lemariva.com/blog/2019/09/raspberry-pi-4b-preempt-rt-kernel-419y-performance-test
https://downloads.raspberrypi.org/raspbian_lite/images/raspbian_lite-2019-07-12/


<!-- ## 安装实用工具

 -->